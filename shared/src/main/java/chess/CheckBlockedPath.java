package chess;

public class CheckBlockedPath {
    //for piece types pawn (on first move) bishop rook queen
    //check all spaces between initial space and target space for any piece (color doesnt matter)
}
